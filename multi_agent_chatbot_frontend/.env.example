# Backend base URL (no trailing slash), e.g. http://localhost:8000
# If omitted, the app will use relative paths (/api/...) and dev proxy from package.json
REACT_APP_BACKEND_URL=http://localhost:8000
