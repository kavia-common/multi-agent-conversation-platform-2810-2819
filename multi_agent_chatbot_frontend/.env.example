# Example environment configuration for the React frontend.
# Copy this file to ".env" and adjust values as needed.
# IMPORTANT: Do not commit your real .env to source control.

# PUBLIC_INTERFACE
# REACT_APP_BACKEND_URL:
# Base URL of the backend API (no trailing slash). If omitted, the app will call
# relative endpoints like /api/chat/message and rely on the "proxy" in package.json
# during local development or same-origin deployment in production.
REACT_APP_BACKEND_URL=http://localhost:8000
